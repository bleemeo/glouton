log:
  opentelemetry:
    grpc:
      enable: true
      port: 4319
    receivers:
      legacy_network:
        include: ["/var/log/mine.log"]
        send_logs: true
opentelemetry:
  listeners:
    legacy-network:
      protocols:
        http:
          endpoint: "0.0.0.0:9999"
