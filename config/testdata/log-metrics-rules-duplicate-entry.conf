log:
  metrics_rules:
    web_errors:
      - metric: web_error_rate
        conditions:
          - 'IsMatch(body, "\\[error\\]")'
      - metric: web_error_rate
        conditions:
          - 'IsMatch(body, "\\[error\\]")'
