log:
  inputs:
    - path: /var/log/apache/access.log
      filters:
        - metric: shared_errors_count
          regex: \[error\]
    - container_name: redis
      filters:
        - metric: shared_errors_count
          regex: ERROR
