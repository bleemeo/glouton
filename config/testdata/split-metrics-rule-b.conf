log:
  metrics_rules:
    apache_to_metrics:
      - metric: log_common_code
        conditions:
          - 'IsMatch(attributes["http.response.status_code"], "3..")'
        labels:
          code: "3xx"
